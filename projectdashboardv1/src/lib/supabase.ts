// Cloud-Sync removed — app runs 100% offline via localStorage.
// Stub kept so no other file needs to change its imports.

export function isSupabaseAvailable(): boolean { return false }
export async function fetchAllEntriesRemote(): Promise<null> { return null }
export async function upsertEntryRemote(): Promise<void> { return }

